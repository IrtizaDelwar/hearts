<div class="col-md-12">
<table class="table table-striped table-dark table-bordered table-sm">
	<thead>
		<tr>
			<th>Game #</th>
			<th>Winner (ELO gained)</th>
			<th>Losers</th>
                        <th>Match ELO</th>
		</tr>
	</thead>

	<tbody>
		<% @game.each do |game| %>
			<tr>
				<td><%= game.game_id %></td>
				<td><%= game.winner %> (+<%= game.elochange.split(",").map(&:to_i).inject(0, :+) %>)</td>
				<td><%= game.losers %></td>
                                <td><%= game.tableelo.split(",").map(&:to_i).inject(0, :+)/4 %></td>
			</tr>
		<% end %>
	</tbody>
</table>
</div>
